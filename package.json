{
  "name": "json-server-demo",
  "version": "1.0.0",
  "description": "how to user json server in localhost",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "nodemon api/server.js",
    "start": "npm run generate-data && node api/server.js",
    "generate-data": "node generate-data.js"
  },
  "keywords": [
    "json-server"
  ],
  "author": "poseidon",
  "license": "MIT",
  "dependencies": {
    "express": "^4.18.2",
    "json-server": "^0.17.4"
  },
  "devDependencies": {
    "@faker-js/faker": "^8.2.0",
    "nodemon": "^3.0.1"
  }
}